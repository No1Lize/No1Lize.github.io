name: Crawler test diagnostic

on:
  pull_request:
    paths:
      - .crawler-test-trigger

permissions:
  contents: write

jobs:
  diagnose:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Record crawler test output
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set +e
          mkdir -p diagnostics
          npm run test:crawler > diagnostics/crawler-test.txt 2>&1
          status=$?
          cat diagnostics/crawler-test.txt
          if [ "$status" -ne 0 ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add diagnostics/crawler-test.txt
            git commit -m "ci: record crawler test failure"
            git push origin "HEAD:${HEAD_REF}"
          fi
          exit "$status"
