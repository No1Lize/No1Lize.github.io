name: Refresh venture profiles

on:
  push:
    branches: [main]
    paths:
      - tools/crawl_venture_profiles.py
      - tools/venture_profile_extraction.py
      - tools/enrich_venture_profiles.py
      - tools/refine_venture_research_evidence.py
      - tools/normalize_venture_profiles.py
      - tools/sanitize_venture_profiles.py
      - tools/sanitize_venture_narratives.py
      - tools/finalize_venture_profiles.py
      - tools/enforce_venture_entity_semantics.py
      - tools/stabilize_venture_profiles.py
      - lib/catalog-data.ts
      - lib/venture-profile-data.ts
      - public/data/articles.json
      - app/companies/**
      - app/institutions/**
      - tests/test_venture_profile_extraction.py
      - tests/test_venture_profile_noise_filters.py
      - tests/test_venture_profile_enrichment.py
      - tests/test_refine_venture_research_evidence.py
      - tests/test_normalize_venture_profiles.py
      - tests/test_venture_profile_postprocess.py
      - tests/test_venture_narrative_sanitizer.py
      - tests/test_finalize_venture_profiles.py
      - tests/test_venture_entity_semantics.py
      - tests/test_stabilize_venture_profiles.py
      - tests/venture-profile-data.test.ts
      - .github/workflows/refresh-venture-profiles.yml
  schedule:
    - cron: "41 3 * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: venture-profile-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Verify venture pipeline and regressions
        run: |
          python -m py_compile \
            tools/venture_profile_extraction.py \
            tools/crawl_venture_profiles.py \
            tools/enrich_venture_profiles.py \
            tools/refine_venture_research_evidence.py \
            tools/normalize_venture_profiles.py \
            tools/sanitize_venture_profiles.py \
            tools/sanitize_venture_narratives.py \
            tools/finalize_venture_profiles.py \
            tools/enforce_venture_entity_semantics.py \
            tools/stabilize_venture_profiles.py
          python -m unittest \
            tests.test_venture_profile_extraction \
            tests.test_venture_profile_noise_filters \
            tests.test_venture_profile_enrichment \
            tests.test_refine_venture_research_evidence \
            tests.test_normalize_venture_profiles \
            tests.test_venture_profile_postprocess \
            tests.test_venture_narrative_sanitizer \
            tests.test_finalize_venture_profiles \
            tests.test_venture_entity_semantics \
            tests.test_stabilize_venture_profiles
      - name: Record catalog revision
        id: catalog-before
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "sha=$(gh api "repos/${GITHUB_REPOSITORY}/contents/lib/catalog-data.ts?ref=main" --jq .sha)" >> "$GITHUB_OUTPUT"
      - name: Crawl all startup and institution profiles
        env:
          VENTURE_PROFILE_USER_AGENT: ${{ vars.VENTURE_PROFILE_USER_AGENT }}
        run: python tools/crawl_venture_profiles.py --workers 8 --max-pages 6
      - name: Enrich profiles with intelligence and capital evidence
        run: python tools/enrich_venture_profiles.py
      - name: Align research fields to entity-specific evidence
        run: |
          python tools/refine_venture_research_evidence.py
          python tools/refine_venture_research_evidence.py --check
      - name: Normalize cross-entity profile consistency
        run: python tools/normalize_venture_profiles.py
      - name: Reach the shared terminal quality fixed point
        run: |
          python tools/stabilize_venture_profiles.py
          python tools/stabilize_venture_profiles.py --check
          python tools/finalize_venture_profiles.py --check
          python tools/enforce_venture_entity_semantics.py --check
      - name: Validate generated venture snapshot
        run: |
          python tools/crawl_venture_profiles.py --validate-only
          python tools/enrich_venture_profiles.py --validate-only
          python tools/refine_venture_research_evidence.py --check
          python tools/stabilize_venture_profiles.py --check
          python tools/finalize_venture_profiles.py --check
          python tools/enforce_venture_entity_semantics.py --check
          python -m unittest \
            tests.test_venture_profile_extraction \
            tests.test_venture_profile_noise_filters \
            tests.test_venture_profile_enrichment \
            tests.test_refine_venture_research_evidence \
            tests.test_normalize_venture_profiles \
            tests.test_venture_profile_postprocess \
            tests.test_venture_narrative_sanitizer \
            tests.test_finalize_venture_profiles \
            tests.test_venture_entity_semantics \
            tests.test_stabilize_venture_profiles
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate venture frontend data model
        run: |
          npm install
          node --import tsx --test \
            tests/venture-profile-data.test.ts \
            tests/catalog.test.ts
      - name: Publish only against the same catalog revision
        id: publish
        env:
          GH_TOKEN: ${{ github.token }}
          CATALOG_BEFORE: ${{ steps.catalog-before.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          CATALOG_AFTER="$(gh api "repos/${GITHUB_REPOSITORY}/contents/lib/catalog-data.ts?ref=main" --jq .sha)"
          if [ "$CATALOG_AFTER" != "$CATALOG_BEFORE" ]; then
            echo "Catalog changed during crawl; refusing to publish a stale profile snapshot."
            exit 1
          fi

          ENDPOINT="repos/${GITHUB_REPOSITORY}/contents/public/data/venture_profiles.json"
          CURRENT_SHA="$(gh api "$ENDPOINT?ref=main" --jq .sha)"
          REQUEST="$(mktemp)"
          python -c 'import base64,json,sys; path,sha,out=sys.argv[1:]; json.dump({"message":"data: refresh startup and institution profiles","content":base64.b64encode(open(path,"rb").read()).decode(),"sha":sha,"branch":"main"},open(out,"w",encoding="utf-8"))' \
            public/data/venture_profiles.json "$CURRENT_SHA" "$REQUEST"
          gh api --method PUT "$ENDPOINT" --input "$REQUEST" >/dev/null
          rm -f "$REQUEST"
          echo "changed=true" >> "$GITHUB_OUTPUT"
      - name: Publish updated site
        if: steps.publish.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run pages.yml --ref main
