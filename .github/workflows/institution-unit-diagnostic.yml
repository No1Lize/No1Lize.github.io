name: Institution unit diagnostic

on:
  pull_request:
    paths:
      - .institution-unit-trigger

permissions:
  contents: write

jobs:
  diagnose:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - name: Record institution unit output
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          set +e
          mkdir -p diagnostics
          node --import tsx --test tests/institution-ranking-data.test.ts > diagnostics/institution-ranking-unit.txt 2>&1
          status=$?
          cat diagnostics/institution-ranking-unit.txt
          if [ "$status" -ne 0 ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add diagnostics/institution-ranking-unit.txt
            git commit -m "ci: record institution ranking unit failure"
            git push origin "HEAD:${HEAD_REF}"
          fi
          exit "$status"
