name: Temporary inspect full refresh runs

# Capture the prior one-time wiring failure. Never merge this workflow.
on:
  pull_request:
    paths:
      - ".github/workflows/temporary-inspect-full-refresh-runs.yml"

permissions:
  contents: read
  actions: read

jobs:
  inspect-wiring-failure:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
    steps:
      - name: Download failed log tail
        run: |
          set -euo pipefail
          gh run view 30318349345 --log-failed > failed.log
          tail -n 240 failed.log | tee failed-tail.log
      - uses: actions/upload-artifact@v4
        with:
          name: bytedance-wiring-failure
          path: |
            failed.log
            failed-tail.log
          if-no-files-found: error
