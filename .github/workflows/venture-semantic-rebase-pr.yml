name: Validate venture semantic rebase

on:
  pull_request:
    branches: [main]
    paths:
      - .venture-semantic-rebase-trigger
      - .github/workflows/venture-semantic-rebase-pr.yml
      - tools/apply_venture_semantic_rebase.py
      - tools/apply_ionq_financing_context_fix.py

permissions:
  contents: write

concurrency:
  group: venture-semantic-rebase-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply semantic rebase
        run: |
          python tools/apply_venture_semantic_rebase.py
          python tools/apply_ionq_financing_context_fix.py
      - name: Compile venture pipeline
        run: |
          python -m py_compile \
            tools/venture_profile_extraction.py \
            tools/crawl_venture_profiles.py \
            tools/enrich_venture_profiles.py \
            tools/refine_venture_research_evidence.py \
            tools/normalize_venture_profiles.py \
            tools/sanitize_venture_profiles.py \
            tools/sanitize_venture_narratives.py \
            tools/finalize_venture_profiles.py \
            tools/enforce_venture_entity_semantics.py \
            tools/stabilize_venture_profiles.py
      - name: Run venture regressions
        run: |
          python -m unittest \
            tests.test_venture_profile_extraction \
            tests.test_venture_profile_noise_filters \
            tests.test_venture_profile_enrichment \
            tests.test_refine_venture_research_evidence \
            tests.test_normalize_venture_profiles \
            tests.test_venture_profile_postprocess \
            tests.test_venture_narrative_sanitizer \
            tests.test_finalize_venture_profiles \
            tests.test_venture_entity_semantics \
            tests.test_stabilize_venture_profiles \
            tests.test_venture_semantic_rebase
      - name: Reprocess latest formal snapshot
        shell: bash
        run: |
          set -euo pipefail
          converged=false
          for pass in $(seq 1 8); do
            cp public/data/venture_profiles.json /tmp/venture-before.json
            python tools/refine_venture_research_evidence.py
            python tools/normalize_venture_profiles.py
            python tools/stabilize_venture_profiles.py
            if cmp -s /tmp/venture-before.json public/data/venture_profiles.json; then
              converged=true
              echo "Complete venture pipeline converged on pass ${pass}."
              break
            fi
          done
          test "$converged" = true
      - name: Check evidence fixed point
        run: python tools/refine_venture_research_evidence.py --check
      - name: Check stabilizer fixed point
        run: python tools/stabilize_venture_profiles.py --check
      - name: Check finalizer fixed point
        run: python tools/finalize_venture_profiles.py --check
      - name: Check entity semantic fixed point
        run: python tools/enforce_venture_entity_semantics.py --check
      - name: Validate crawler snapshot contract
        run: python tools/crawl_venture_profiles.py --validate-only
      - name: Validate enrichment snapshot contract
        run: python tools/enrich_venture_profiles.py --validate-only
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate frontend consumption
        run: |
          npm install
          node --import tsx --test tests/venture-profile-data.test.ts tests/catalog.test.ts
      - name: Commit validated rebase and clean temporary files
        shell: bash
        run: |
          set -euo pipefail
          rm -f \
            tools/apply_venture_semantic_rebase.py \
            tools/apply_ionq_financing_context_fix.py \
            .github/workflows/venture-semantic-rebase-pr.yml \
            .github/workflows/venture-semantic-diff-pr.yml \
            .venture-semantic-rebase-trigger
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            tools/refine_venture_research_evidence.py \
            tools/finalize_venture_profiles.py \
            tools/enforce_venture_entity_semantics.py \
            tests/test_refine_venture_research_evidence.py \
            tests/test_finalize_venture_profiles.py \
            tests/test_venture_semantic_rebase.py \
            lib/catalog-data.ts \
            public/data/venture_profiles.json
          git add -u \
            tools/apply_venture_semantic_rebase.py \
            tools/apply_ionq_financing_context_fix.py \
            .github/workflows/venture-semantic-rebase-pr.yml \
            .github/workflows/venture-semantic-diff-pr.yml \
            .venture-semantic-rebase-trigger
          git commit -m 'fix: rebase venture semantic gates on latest main'
          git push origin HEAD:${{ github.head_ref }}
