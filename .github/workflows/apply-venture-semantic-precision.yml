name: Apply venture semantic precision

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-venture-semantic-precision.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply semantic precision rules
        run: |
          python tools/apply_venture_semantic_precision_patch.py
          python -m py_compile tools/venture_profile_extraction.py tools/crawl_venture_profiles.py
          python -m unittest \
            tests.test_venture_profile_extraction \
            tests.test_venture_profile_noise_filters
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tools/venture_profile_extraction.py
          git commit -m 'fix: tighten venture team and product semantics'
          git pull --rebase origin main
          python -m py_compile tools/venture_profile_extraction.py tools/crawl_venture_profiles.py
          git push origin HEAD:main
